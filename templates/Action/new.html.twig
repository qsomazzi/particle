{% extends 'base.html.twig' %}

{% block headerPretitle %}{% if admin.domain is not null%}{{ admin.domain }}{% endif %}{% endblock %}
{% block headerTitle %}New {{ admin.singularLabel }}{% endblock %}

{% block headerActions %}
    <twig:button href="{{ path('admin_' ~ identifier) }}">
        <i class="ti ti-arrow-left"></i>
        Back to list
    </twig:button>
{% endblock %}

{% block content %}
    <div class="row row-deck row-cards">
        <div class="col-12">
            {{ include('@Particle/Action/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
