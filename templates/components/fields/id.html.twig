{% if display == constant('Qsomazzi\\Particle\\Admin\\AdminInterface::ACTION_LIST') %}
    <td class="text-secondary">{{ value }}</td>
{% else %}
    {# ID field is already displayed in the panel, no need to display it a second time here #}
{% endif %}
